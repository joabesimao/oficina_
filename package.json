{
  "name": "oficina",
  "version": "1.0.0",
  "description": "api oficina de carros",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "lite-server --baseDir=dist",
    "start": "concurrently \"npm run watch\" \"npm run server\"",
    "compile": "tsc",
    "watch": "tsc -w"
  },
  "author": "Joabe Simao",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0",
    "lite-server": "^2.6.1",
    "typescript": "^4.9.4"
  }
}
